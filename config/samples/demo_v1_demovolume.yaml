apiVersion: demo.demo.tobi.io/v1
kind: DemoVolume
metadata:
  labels:
    app.kubernetes.io/name: project
    app.kubernetes.io/managed-by: kustomize
  name: demovolume-sample
spec:
  name: demo
  size: 1
  # TODO(user): Add fields here
